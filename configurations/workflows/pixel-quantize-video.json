{
  "id": "pixel-quantize-video",
  "workflow": "pixel-quantize-video",
  "friendly_name": "Pixel Quantize",
  "effect_type": "Pixelize",
  "default": true,
  "params": {
    "virtual_resolution": {
      "value": 128,
      "visibility": "exposed",
      "min": 32,
      "max": 1024
    },
    "grid_w": {
      "value": 384,
      "visibility": "hidden"
    },
    "grid_h": {
      "value": 256,
      "visibility": "hidden"
    },
    "palette": {
      "value": "chroma-256",
      "visibility": "exposed"
    },
    "final_method": {
      "value": "median",
      "visibility": "exposed"
    },
    "fps": {
      "value": 0,
      "visibility": "exposed",
      "min": 0,
      "max": 60
    },
    "engine": {
      "value": "median",
      "visibility": "hidden"
    }
  },
  "card": {
    "architecture": "Deterministic OKLab palette + grid quantizer applied per frame (no model) — LoadVideo → GetVideoComponents → PixelQuantize → SaveAnimatedWEBP",
    "summary": "Pixel-quantizes every frame of a source clip onto a fixed grid + palette and re-encodes the clip at its own frame rate. No model, no VRAM. A locked (named) palette is the default so the colors are identical every frame — temporally consistent, no flicker. This is the only editor offered when the source is a video.",
    "use_cases": [
      "turn a generated or uploaded clip into pixel-art video",
      "deterministic frame-by-frame quantization with no diffusion redraw"
    ],
    "nsfw_capable": "n/a (no model)",
    "pick_when": "You have a video clip and want it pixel-quantized frame by frame."
  }
}
