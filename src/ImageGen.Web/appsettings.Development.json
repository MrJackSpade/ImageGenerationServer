{
  "_comment": "Dev overrides the archive-root (../) defaults from appsettings.json so a plain `dotnet run` keeps its SQLite file inside the project and does not write logs above it. The file sink is off in dev (console only).",
  "ConnectionStrings": {
    "ImageGen": "Data Source=imagegen.db"
  },
  "Logging": {
    "FilePath": "",
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
